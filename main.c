#include "main.h"
/**
* _memset -  function fills the first n bytes of the memory area pointed
* @s: array
* @b: value
* @n: number of bytes
* Return: value of *s
*/
int main(int argc, char **argv)
{
	printf("$ ");
	fflush(stdout);
	int i = 0;
	while (argv[i] != '\n')
	{

		i++;
	}
}
